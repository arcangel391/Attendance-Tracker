<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendancetracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"

        android:theme="@style/Theme.AttendanceTracker"
        android:usesCleartextTraffic="true">

        <activity android:name=".AttendanceLog"/>
        <activity android:name=".AnnouncementsDetails"/>
        <activity android:name=".Navbar">

        <!-- <activity
           android:name=".TeamMonitoring"
            android:exported="false" />

         <activity
            android:name=".Navbar"
            android:exported="false" />
        <activity android:name=".LoginPage" />
        -->
        <!--
<activity
            android:name=".SplashScreen"
            android:exported="false" />
        -->
        <activity
            android:name=".Dashboard"
            android:exported="false"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/Theme.AttendanceTracker.NoActionBar" />
        <activity android:name=".TeamMonitoring">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>